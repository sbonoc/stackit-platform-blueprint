export QUALITY_HOOKS_KEEP_GOING=true

# Cross-reference the canonical Python version from versions.sh so uv resolves
# the correct interpreter without a separate .python-version file to maintain.
# shellcheck source=scripts/lib/platform/apps/versions.sh
source scripts/lib/platform/apps/versions.sh
export UV_PYTHON="${PYTHON_RUNTIME_BASE_IMAGE_VERSION}"
