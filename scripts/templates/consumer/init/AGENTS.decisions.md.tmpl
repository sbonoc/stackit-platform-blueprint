# Decisions Log

## Initial Baseline
- Repository created from STACKIT Platform Blueprint template version `{{TEMPLATE_VERSION}}`.
- Default work scope is project delivery: `apps/**`, `docs/platform/**`, `make/platform*.mk`, `scripts/bin/platform/**`, and `scripts/lib/platform/**`.
- Blueprint-managed paths are changed intentionally and only when bootstrap, contract, validation, or inherited blueprint behavior is part of the task.
