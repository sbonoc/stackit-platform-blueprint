fullnameOverride: "{{POSTGRES_HELM_RELEASE}}"
auth:
  existingSecret: "{{POSTGRES_CREDENTIAL_SECRET_NAME}}"
  database: "{{POSTGRES_DB_NAME}}"
image:
  registry: "{{POSTGRES_IMAGE_REGISTRY}}"
  repository: "{{POSTGRES_IMAGE_REPOSITORY}}"
  tag: "{{POSTGRES_IMAGE_TAG}}"
primary:
  persistence:
    enabled: false
