fullnameOverride: "{{RABBITMQ_HELM_RELEASE}}"
auth:
  username: "{{RABBITMQ_USERNAME}}"
  existingPasswordSecret: "{{RABBITMQ_PASSWORD_SECRET_NAME}}"
  existingSecretPasswordKey: rabbitmq-password
service:
  type: ClusterIP
