fullnameOverride: "blueprint-vault"
server:
  dev:
    enabled: true
    devRootToken: "{{KMS_VAULT_ROOT_TOKEN}}"
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"
injector:
  enabled: false
