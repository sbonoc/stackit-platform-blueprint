# Observability Tooling Tests

This folder is reserved for observability-specific tooling tests when the module surface grows.
Current module behavior is validated in `tests/infra/test_optional_modules.py`.
