# Hardening Review

## Repository-Wide Findings Fixed
- Finding 1: Slice 6 contract drift in `tests/blueprint/contract_refactor_docs_cases.py` — the fixture asserted a stale extension count after `template_sync_allowlist` grew by the three new `docs/blueprint/autonomous-factory/` documents. Repaired in commit `afa3be9` so the contract test reflects the actual allowlist surface rather than the pre-FR-019 count; prevents a green/red split between local runs and CI.
- Finding 2: MDX-breaking literal braces in `ADR-issue-337-llm-model-router-policy.md` and `ADR-issue-337-triage-size-threshold.md` — the originals embedded bare `{Opus, Sonnet, Haiku}` and `{bounded-context, ...}` enumerations that Docusaurus MDX 3 parses as expression containers and rejects. Wrapped both in inline-code backticks in commit `a1f9da0` so the `make docs-build` regression is permanently closed for any future ADR that lists set-notation enumerations.
- Finding 3: Stale "implemented as" annotations on T-002/T-003/T-005 in `tasks.md` — prior session's compaction summary had recorded alternate filenames that do not exist on disk; corrected `tasks.md` in this work item's Step 06 pass so the per-task evidence trail cites the actual filenames now living under `docs/blueprint/architecture/decisions/`. No code impact; eliminates a documented source of confusion for any reviewer auditing #339 C8 enumeration.

## Observability and Diagnostics Changes
- Metrics/logging/tracing updates: none in runtime code (Phase 0 ships zero runtime). The instrumentation **plan** (`docs/blueprint/autonomous-factory/instrumentation-plan.md`) declares the FR-012 primary metric (P50 lead time), three guardrail metrics, four data sources, the `stackit-managed-grafana` dashboard target, 13-month retention parity with #334 LogMe WORM, and the per-`owner_team` breakdown shape — all sourced over the #339 Contract C7 lifecycle event stream per NFR-OBS-001. Implementation of these metrics is owned by Phase 1 tickets #335 (LiteLLM gateway + emission) and #336 (GitHub Actions webhook bridge).
- Operational diagnostics updates: `docs/blueprint/autonomous-factory/pre-factory-baselines.md` records the four FR-014 baseline metrics measured over the SDD-enabled window (2026-04-17 → 2026-05-27) so post-factory dashboards have a deterministic comparison point at week one.

## Architecture and Code Quality Compliance
- SOLID / Clean Architecture / Clean Code / DDD checks: N/A for this work item beyond documentation prose; no runtime code is introduced. The ten ADRs enforce the architectural boundaries themselves (router policy, sovereignty posture, SoD, reject-rerun cap, ceilings, reviewer heterogeneity, triage threshold, light decomposition) that Phase 1 implementation work will be measured against.
- Test-automation and pyramid checks: N/A (no code units, no API/contract/filter surface introduced). `make docs-build` + `make docs-smoke` cover the documentation surface; `make quality-sdd-check` covers the SDD asset surface; `make blueprint-test-unit` (1074 tests + 42 subtests) confirms zero schema fallout from the new `sod_policy` block under `spec.spec_driven_development_contract.readiness_gate`.
- Documentation/diagram/CI/skill consistency checks: all ten ADRs and the three autonomous-factory documents discoverable via the autogenerated Docusaurus sidebar (`docs/sidebars.js` uses `dirName: "blueprint"`); the bootstrap mirror under `scripts/templates/blueprint/bootstrap/docs/blueprint/autonomous-factory/` contains byte-identical copies of the three consumer-shipped documents (`sync_blueprint_template_docs.py` confirms zero diff: `created=0 updated=0 removed=0 skipped=17`); the meta-ADR + design-contracts.md C6/C7/C8 updates cite the ten ADRs and three documents by relative path. #339 design-contracts evidence_manifest.json SHA-256 entries regenerated for the modified surface.

## Accessibility Gate (Normative — non-UI reviewers mark non-applicable items N/A)
- [x] SC 4.1.2 (Name, Role, Value): N/A — no UI surface (governance documentation only).
- [x] SC 2.1.1 (Keyboard): N/A — no UI surface.
- [x] SC 2.4.7 (Focus Visible): N/A — no UI surface.
- [x] SC 1.4.1 (Use of Color): N/A — no UI surface.
- [x] SC 3.3.1 (Error Identification): N/A — no UI surface.
- [x] axe-core WCAG 2.1 AA scan evidence: N/A — no UI surface; NFR-A11Y-001 declared N/A in `spec.md` with rationale.

## Proposals Only (Not Implemented)
- Proposal 1: Operational team provisioning on GitHub (deferring ticket: #337; resolve-by deadline: #337 close, per design-contracts.md C6 Open Decisions). The eight teams named in `.github/CODEOWNERS` and pinned in design-contracts.md C6 `### Blueprint instance` (gate-1: `factory-product`, `factory-architecture`, `factory-security`, `factory-operations`; gate-2: `factory`, `infra`, `docs`, `governance`) must be created on GitHub under the `@sbonoc` org and populated with ≥ 2 members each before any factory `agent-ready` label is applied. This is an operational task, not a code change; Operations sign-off on this PR attests to the prerequisite. Tracked as parked in `AGENTS.backlog.md` under `after: github-teams-provisioned`.
- Proposal 2: Solo-operator profile enforcement layer for Phase 1 #336 (deferring ticket: this PR's FR-005 amendment; resolve-by deadline: #336 implementation start). The FR-005 ADR amendment (`§ Satisfaction paths`) defines Path 2 (solo-operator) declaratively, and `blueprint/contract.yaml` carries the `sod_policy.solo_operator` toggle + canonical attestation marker + checklist item count under `spec.spec_driven_development_contract.readiness_gate`. No enforcement code yet exists — the GitHub Actions webhook check that reads the toggle, scans the PR for the literal `SOLO_OPERATOR_ATTESTATION: confirmed` marker on a canonical comment, validates the five-item checklist, and applies the SoD count adjustment is owned by Phase 1 #336. Tracked as parked in `AGENTS.backlog.md` under `after: issue-336`.
