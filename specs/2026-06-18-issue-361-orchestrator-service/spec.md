# Specification

## Spec Readiness Gate (Blocking)
<!-- SPEC_PRODUCT_READY=true: intake gate — Product sign-off only; unlocks agent ADR drafting.
     SPEC_READY=true: implementation gate — all sign-offs required; unlocks coding. -->
- SPEC_READY: true
- SPEC_PRODUCT_READY: true
- Open questions count: 0
- Unresolved alternatives count: 0
- Unresolved TODO markers count: 0
- Pending assumptions count: 0
- Open clarification markers count: 0
- Product sign-off: approved
- Architecture sign-off: approved
- Security sign-off: approved
- Operations sign-off: approved
- Missing input blocker token: none
- ADR path: docs/blueprint/architecture/decisions/ADR-issue-361-orchestrator-service.md
- ADR status: approved
- SPEC_READY_EXCEPTION: none
- authorized-by: none

## Applicable Guardrail Controls (Normative)
- Applicable control IDs: SDD-C-001, SDD-C-002, SDD-C-003, SDD-C-004, SDD-C-005, SDD-C-006, SDD-C-007, SDD-C-008, SDD-C-009, SDD-C-010, SDD-C-011, SDD-C-012, SDD-C-013, SDD-C-016, SDD-C-017, SDD-C-019, SDD-C-020, SDD-C-021
- Control exception rationale: SDD-C-014 (local-first runtime) does not apply — the orchestrator runs on the SKE control-plane node pool, not on local Docker Desktop; local dev parity is provided by an in-process stub adapter, not a containerized local lane. SDD-C-015 (app onboarding Make-target contract) does not apply — the orchestrator is platform/factory infrastructure, not an app-delivery workload. SDD-C-018 (upstream defect escalation) does not apply — no blueprint defect workaround. SDD-C-022 (HTTP smoke gate) does not apply — the orchestrator exposes no HTTP route handlers; its external surfaces are a RabbitMQ subscriber, an OpenHands Agent Server API client, and a durable-bus publisher. SDD-C-023 (filter/payload positive-path assertion) does not apply — no HTTP filter/payload-transform logic. SDD-C-024 (finding-to-test translation) remains applicable; pre-PR findings on the orchestrator MUST be translated into failing automated tests first.

## Implementation Stack Profile (Normative)
- Backend stack profile: python_plus_fastapi_pydantic_v2
- Frontend stack profile: none
- Test automation profile: pytest_vitest_playwright_pact
- Agent execution model: specialized-subagents-isolated-worktrees
- Managed service preference: stackit-managed-first
- Managed service exception rationale: none
- Runtime profile: local-first-docker-desktop-kubernetes
- Local Kubernetes context policy: docker-desktop-preferred
- Local provisioning stack: crossplane-plus-helm
- Runtime identity baseline: eso-plus-argocd-plus-keycloak
- Local-first exception rationale: The orchestrator's production target is the SKE control-plane node pool factory namespace; local-lane parity is provided by an in-process Python adapter (RabbitMQ + OpenHands stubs) rather than a containerized local cluster lane, because the orchestrator's runtime targets (OpenHands Agent Server per #335 — self-hosted in-cluster Helm chart deployed via ArgoCD, sibling pattern to Keycloak; the RabbitMQ trigger queue per #336) are owned by sibling tickets whose reusable Helm charts have not yet shipped. Until those charts exist, the orchestrator's local-cluster smoke lane cannot pull them in. Container packaging is shipped via this work item's Helm chart in `#361.4`; local-cluster smoke is deferred to a follow-up once #335 + #336 ship.
- Has user-facing flow: false <!-- inferred from intake: no UI/flow keywords or labels in issue #361 — confirm before SPEC_READY -->
  <!-- Signal list — set true if the issue or FR text mentions ANY of: form, wizard, modal,
       dialog, page, screen, UI, frontend, browser, user journey, onboarding, dashboard,
       button, input, component, flow, checkout, login, signup, profile, settings, view,
       layout, render, display; labels: frontend, ui, ux, web, accessibility; any frontend
       framework name. A non-none frontend-stack-profile always implies true. -->
- E2E gate classification: N/A
  <!-- No user-facing flow → gate does not apply. The orchestrator is a headless Python
       Deployment with no browser surface; its observability is via C7 events + Grafana,
       authored under #350 (downstream of this work item). -->

## Objective
- Business outcome: Ship the **orchestrator service** — the persistent Python `Deployment` that runs in the factory namespace on the SKE control-plane node pool and drives every autonomous factory execution. The orchestrator is one of exactly three C7 emitters (per `docs/blueprint/architecture/decisions/ADR-issue-337-c7-emission-mechanism.md` and `ADR-issue-347-human-sdd-c7-symmetry.md`), owns phase-boundary C7 event emission, subscribes to the #336 webhook handler's trigger queue, invokes the OpenHands Agent Server (#335) per the SDD-step × expert dispatch matrix in `docs/blueprint/autonomous-factory/design-contracts.md` § Contract C3, validates skill outputs against the YAML jsonschema authored in each `SKILL.md` `## Required Output Schema` block (#360), and packages itself as a reusable Helm chart under `scripts/templates/infra/orchestrator/` per Contract C8.
- Success metric: A `phase: intake` C7 event for a factory work-item is emitted by the orchestrator (`emitter: orchestrator`) to the durable bus with all eleven sealed minimum fields populated and a deterministic `event_id = sha256(ticket_id|phase|rerun_round|emitter)`, validated against the C7 schema in `docs/blueprint/autonomous-factory/design-contracts.md` § Contract C7; the same event carries the additive extension fields `outcome_details.expert_verdicts[]`, `outcome_details.routing_keys`, `outcome_details.token_usage`, and `outcome_details.merger_overhead` per ADR-issue-364 § 9 and ADR-issue-368.

## Normative Requirements

### Functional Requirements (Normative)
- FR-001 The work item MUST author a parent coordination spec at `specs/2026-06-18-issue-361-orchestrator-service/` that decomposes `#361` into 5 child work items along the boundary types declared in `docs/blueprint/architecture/decisions/ADR-issue-337-light-decomposition-policy.md`. Each child MUST be filed as a separate GitHub issue, MUST cite this parent spec path AND its canonical boundary type + specific boundary value in its body (per `ADR-issue-337-light-decomposition-policy.md` § Allowed boundary types — the boundary type MUST be EXACTLY ONE OF `bounded-context`, `architectural-layer`, or `user-visible-feature-behavior`; no other vocabulary is permitted by the policy), AND MUST receive an independent SDD intake when its blocker chain is clear. **Multi-axis decomposition (authorized exception per ADR-issue-337-light-decomposition-policy "Multi-axis exception for manually-authored parent coordination specs" amended 2026-06-19).** This parent coordination spec is authored MANUALLY by a human spec author, NOT through a `blueprint-ticket-decompose-light` runtime invocation (the skill itself is being authored by this very epic — it cannot run on its own meta-decomposition). The parent scope legitimately spans two distinct boundary axes (runtime architecture + governance/expert-panel contract), and the ADR exception explicitly permits this case provided every child cites EXACTLY ONE canonical boundary type. The 5 children + their canonical (axis, value) pairs are: (a) `#361.1` Dispatch + convergence + schema validator + predicate-registry mechanism (boundary type: `architectural-layer`, value: `domain/application — pure-Python core, no I/O`); (b) `#361.2` C7 emitter + bus publisher + reviewer-rotation picker (boundary type: `architectural-layer`, value: `infrastructure — emission and bus integration`); (c) `#361.3` OpenHands API client + RabbitMQ trigger subscriber + work loop (boundary type: `architectural-layer`, value: `infrastructure — external-runtime clients and service entrypoint`); (d) `#361.4` Helm chart + NetworkPolicy + ESO + ServiceAccount (boundary type: `architectural-layer`, value: `infrastructure — deployment surface`); (e) `#361.5` `usability-pragmatist` PERSONA.md + C3 matrix wiring + AGENTS.backlog `#369` closure (boundary type: `bounded-context`, value: `expert-panel — the persona file IS its own bounded context distinct from the orchestrator runtime per ADR-issue-364 three-layer model, with its own review surface, Architecture+Product reviewer ownership, and ADR-amendment authoring contract`). Rationale for needing two axes: the runtime/governance split is the very precedent the skill cannot establish on itself; classifying `#361.5` honestly as `bounded-context` (rather than bending it to `architectural-layer: interface`) is the strategic-correctness fix landed per PR #372 strategic-alignment audit (Epic #332 alignment). The skill remains single-axis-only at runtime; this manual exception does NOT establish a precedent for `blueprint-ticket-decompose-light` invocations.
- FR-002 The orchestrator MUST load the SDD-step × expert dispatch matrix from `docs/blueprint/autonomous-factory/design-contracts.md` § Contract C3 at startup and MUST NOT hardcode the matrix in service code. The loader MUST parse the contract markdown into a typed in-memory mapping `step → { skill, expert_slugs[], lead_voice, convergence_mode }` and MUST validate each `expert_slug` against the C7 two-sub-enum field defined in `design-contracts.md` § C7 F-12 clarification (amended 2026-06-19): an `expert_slug` is valid if it satisfies EXACTLY ONE OF (a) presence in the sealed `expert_slug_blueprint` enum loaded from `design-contracts.md`, OR (b) presence in the consumer-overlay `expert_slug_extension` allowlist loaded from `blueprint/contract.yaml` § `factory_contract.expert_panel_extensions[]` (the consumer-extension pattern that avoids per-addition `#339` sign-off cost). Skill basenames MUST resolve to `.agents/skills/` directories. The loader MUST reject startup on any unresolvable matrix row. **The enum sources MUST be read at startup** — values MUST NOT be hard-coded in service code (which would re-introduce the per-addition sign-off problem the F-12 extension pattern was authored to solve). The dispatch matrix loader is owned by child `#361.1`.
- FR-003 The orchestrator MUST implement the three convergence modes defined in `docs/blueprint/architecture/decisions/ADR-issue-364-expert-persona-model.md` § 4 / § 5: (a) `parallel-then-merge` (default; steps 01/02/04/06/07): N panel members run independently, the merger applies precedence `block > revise > pass`, finding-text deduplication is naive string-equality at v1 (embedding-based dedup is explicitly deferred per ADR-issue-364 § 11 and the AGENTS.backlog after-issue-368 entry); (b) `sequential-lens` (step05 only): each expert sees the prior expert's deltas in the documented application order from § C3 row 5; (c) `structured-disagreement` (step08 only): experts respond to each other's verdicts before the orchestrator merges. The convergence engine is owned by child `#361.1`.
- FR-004 The orchestrator MUST validate every skill execution output against the YAML jsonschema declared in that skill's `SKILL.md` `## Required Output Schema` fenced block (authored in `#360`) BEFORE emitting any C7 event. Validation failures MUST NOT emit a C7 event; the orchestrator MUST surface the failure via the OpenHands execution log AND through the `#336` webhook handler's reject-rerun cap path. The schema validator is owned by child `#361.1`.
- FR-005 The orchestrator MUST emit every phase-boundary C7 lifecycle event with the eleven sealed minimum fields enumerated in `docs/blueprint/autonomous-factory/design-contracts.md` § Contract C7, deriving `event_id = sha256(ticket_id|phase|rerun_round|emitter)` (the four-input orchestrator form per `ADR-issue-337-c7-emission-mechanism.md` § Emission idempotency). The orchestrator MUST set `emitter: orchestrator` and `persona` to the draft-producing skill basename (per the semantic shift recorded in § Contract C7 `persona` description). The orchestrator MUST populate the additive extension field `outcome_details.touches_services[]` on every phase-boundary event, reading the service set from the work-item `spec.md` Implementation Stack Profile + `## Contract Changes` § API contract section at emission time (empty array when the work item touches no service — e.g., docs-only or governance-only work; the Central Brain #343 ingest pipeline depends on this field for Journey C SRE-incident correlation). Phase-boundary emission MUST be exactly-once per `(ticket_id, phase, rerun_round)` tuple; idempotent re-emission with the same `event_id` MUST be tolerated at the bus layer. C7 emission is owned by child `#361.2`.
- FR-006 The orchestrator MUST populate the standardized additive extension fields on every panel-dispatched C7 event (steps with ≥ 2 experts): (a) `outcome_details.expert_verdicts[]` — one `ExpertVerdictSummary` row per dispatched expert per ADR-issue-364 § 9, with `expert_slug` populated as the two-sub-enum form (`expert_slug_blueprint` OR `expert_slug_extension`) per design-contracts.md § C7 F-12 clarification (amended 2026-06-19); (b) `outcome_details.routing_keys` — set of LiteLLM routing keys used across the panel for the `(ticket_id, phase, rerun_round)` tuple, covering all phases per ADR-issue-368; (c) `outcome_details.token_usage` — per-expert `{input_tokens, output_tokens, routing_key}` map (the `routing_key` sub-key MUST be the LiteLLM routing key actually used for that expert's invocation, enabling per-`model_family` cost decomposition for the per-ticket cost-ceiling audit + Central Brain #343 Journey C cost-attribution queries; this is the F2 amendment per PR #372 strategic-alignment audit — per-expert-only decomposition would be lossy because the same expert can run on different model families across rounds and cost is model-priced). Sentinel `{input_tokens: -1, output_tokens: -1, routing_key: "unknown"}` when LiteLLM `usage` is unavailable; (d) `outcome_details.merger_overhead` — `{findings_before_dedup, findings_after_dedup, severity_escalation_events}`. The orchestrator MUST also emit `outcome_details.ticket_token_summary` on `phase: agent-pr-review` events using the in-process per-ticket token accumulator (NOT JSONL read-back — orchestrator emits to durable bus). Extension-field population is owned by child `#361.2`.
- FR-007 The orchestrator MUST implement the FR-008 reviewer-rotation picker: at `phase: agent-pr-review` dispatch the picker MUST query the durable bus for the most recent C7 event with `phase: implement` on the same `ticket_id`, normalize the event's `model` field via `model_family(s)` per § C7, and select panel routing keys such that BOTH (a) `model_family(implement.model)` is NOT the sole family represented in the panel routing-key set AND (b) the panel routing-key set carries ≥ 2 distinct families. Absence of an `implement` event MUST default to a deterministic tie-break per `ADR-issue-337-reviewer-model-heterogeneity.md`. The reviewer-rotation picker is owned by child `#361.2`.
- FR-008 The orchestrator MUST subscribe to the RabbitMQ work-queue fronted by the `#336` in-cluster webhook-receiver service (the trigger-handoff transport is intentionally separated from the C7 observability stream per `ADR-issue-337-c7-emission-mechanism.md`; per #336, the receiver is an in-cluster Python service that GitHub Actions reusable workflows POST to over authenticated HTTPS — direct publish from GitHub-hosted runners is impossible without public RabbitMQ exposure, rejected by NFR-SEC-001 + #334 egress NetworkPolicy). The orchestrator's contract is "consume from a RabbitMQ work queue" and it is agnostic to the publisher topology upstream of the queue. For each `trigger-accepted` message the orchestrator MUST invoke the OpenHands Agent Server API (`#335`) with the persona/model/skill selection per FR-002 and the model-router policy from `ADR-issue-337-model-router-policy.md`. The subscriber and OpenHands API client are owned by child `#361.3`.
- FR-009 The orchestrator MUST NOT carry Anthropic API keys nor any direct provider credentials. All model invocations MUST route through LiteLLM (the gateway is owned by `#335`). Credential references MUST be supplied via ESO-managed Kubernetes Secrets mounted into the orchestrator pod; the Helm chart `values.yaml` MUST expose the ESO secret-store reference name only, not the credential value. ESO integration is owned by child `#361.4`.
- FR-010 The orchestrator MUST ship as a reusable Helm chart at the canonical path `scripts/templates/infra/orchestrator/` (NOT under `scripts/templates/infra/module_wrappers/` — that subdirectory is reserved for Terraform module wrappers around STACKIT-managed services like `dns`, `kms`, `secrets-manager`, `observability`, etc. The orchestrator is a cluster-resident Python `Deployment`, not a Terraform module wrapper around a STACKIT-managed service). The chart MUST be enumerated in `docs/blueprint/autonomous-factory/design-contracts.md` § Contract C8 § Category (b) — Terraform/Helm module wrappers — with stability tier `stable` and owning ticket `#361.4`. The chart `values.yaml` MUST expose (at minimum): durable-bus connection URL (RabbitMQ AMQP), OpenHands Agent Server URL, factory bot identity (Kubernetes `ServiceAccount` name + ESO-managed credential reference name), model-router config-map name, reviewer-rotation policy config-map name. The Helm chart is owned by child `#361.4`.
- FR-011 An egress `NetworkPolicy` on the factory namespace MUST permit the orchestrator pod to reach EXACTLY THREE destination categories: (a) the durable bus (RabbitMQ in-cluster service), (b) the OpenHands Agent Server in-cluster service, (c) STACKIT internal endpoints required by ESO secret resolution. Public-internet egress MUST be DENIED. NetworkPolicy authoring is owned by child `#361.4`.
- FR-012 The orchestrator MUST support conditional dispatch on the C3 matrix: panel rows MUST be permitted to declare a predicate that the orchestrator evaluates against the in-context work item state before dispatching; rows whose predicate evaluates false MUST be skipped without emitting a `pass` verdict for the gated expert. **C3 matrix schema change.** The C3 matrix table in `docs/blueprint/autonomous-factory/design-contracts.md` MUST be extended with EXACTLY ONE new column `Predicate` as the 6th column (after `Convergence mode`). For the 8 pre-existing rows authored at intake of `#339`, the column value MUST be the literal string `none` (preserving existing dispatch behavior — every expert in those rows fires unconditionally). The schema change is authored once in `#361.5`'s PR (the child that introduces the first non-`none` predicate value); `#361.1`'s `MatrixRow` typed shape MUST treat the column as `predicate: str | None` where the literal string `none` parses to Python `None`. The first non-`none` predicate to ship is named `ui-fidelity-or-a11y` and evaluates as a DISJUNCTION (OR) so it fires on both new-user-facing-flow work AND accessibility-only changes to existing surfaces: `( has-user-facing-flow: true AND diff-touches-frontend-paths ) OR diff-touches-accessibility-relevant-paths`. The OR-clause captures the case where an existing surface is being made more accessible (CSS contrast fix, ARIA semantics addition, focus-management bug fix, axe-violation remediation) in a PR whose `spec.md` correctly declares `has-user-facing-flow: false` because no NEW flow is introduced — the strict-AND form would silently let those PRs ship without `usability-pragmatist` review, which is precisely the case where the persona matters most. The orchestrator's predicate registry exposes the evaluator with TWO independently consumer-extensible regex sets in the config-map (`predicates.frontend.pathRegex` default `^(touchpoints/|frontend/|.*\.vue|.*\.tsx)$`, AND `predicates.accessibility.pathRegex` default `^(.*\.(css|scss)|.*aria.*|.*axe.*|.*a11y.*|.*accessibility.*)$`) so consumers with different layouts can override each clause independently without forking the chart. The `usability-pragmatist` expert MUST be added as the 9th standing PERSONA.md under `.agents/personas/usability-pragmatist/PERSONA.md`, MUST be wired into the C3 dispatch matrix at the SDD steps where UI/UX/accessibility scope matters (at minimum: step01, step04, step05, step08), and MUST be gated by the `ui-fidelity-or-a11y` predicate (declared in the new `Predicate` column for each of the `usability-pragmatist` rows). **C7 expert_slug_blueprint enum amendment.** Because `design-contracts.md` § C7 F-12 clarification (amended 2026-06-19 per PR #372 strategic-alignment audit) splits `expert_slug` into two sub-enums (`expert_slug_blueprint` sealed at the standing blueprint roster; `expert_slug_extension` consumer-overlay allowlist), `#361.5` MUST ALSO author the paired amendment to `design-contracts.md` § C7 that widens `expert_slug_blueprint` from 8 to 9 (admitting `usability-pragmatist`) via the standard `#339` sign-off cycle. `usability-pragmatist` lands in `expert_slug_blueprint` (not `expert_slug_extension`) because it is a blueprint-baseline expert shipped to all consumers, not a per-consumer overlay. Without this paired amendment the orchestrator's FR-002 loader rejects `usability-pragmatist` at startup AND the C7 emitter constructs events that fail schema validation — `#361.5` is dead-on-arrival without it (the cross-cutting dependency Codex flagged on PR #372 4th-review). Conditional dispatch + `usability-pragmatist` expert + the paired C7 enum amendment close the previously deferred `#369` follow-on. Future blueprint expert additions beyond `usability-pragmatist` continue to require a `#339` cycle for `expert_slug_blueprint` widening; future consumer-specific expert additions go through `expert_slug_extension` without a sign-off cycle (the extension-enum pattern authored at PR #372 to eliminate the per-addition compounding cost). Conditional dispatch is owned by child `#361.1` (predicate-registry mechanism + `MatrixRow.predicate` parser) and `#361.5` (PERSONA.md + C3 matrix wiring including the C3 schema change + the C7 `expert_slug_blueprint` widening amendment via `#339` sign-off cycle + `#369` closure).
- FR-013 The parent issue `#361` MUST carry an `## Integration Acceptance Criteria` section that lists checkboxes only satisfiable by cross-child behavior, per `docs/blueprint/autonomous-factory/design-contracts.md` § Contract C4. Parent closure MUST require all five child issues merged AND every integration checkbox ticked by a human bounded-context reviewer (the factory bot MUST NOT tick these). The integration AC enumeration is in `## Normative Acceptance Criteria` below (AC-005 through AC-009).
- FR-014 This work item MUST author an idempotent helper script at `specs/2026-06-18-issue-361-orchestrator-service/file_children.sh` that files EXACTLY four child GitHub issues via `gh issue create`: `#361.1` (dispatch + convergence + schema validator + predicate-registry mechanism), `#361.2` (C7 emitter + bus publisher + reviewer-rotation picker), `#361.4` (Helm chart + NetworkPolicy + ESO + ServiceAccount), `#361.5` (usability-pragmatist PERSONA.md + C3 matrix wiring + `#369` closure). The script MUST NOT file `#361.3` (deferred per Q-1 until `#335` + `#336` reach spec-complete). Each issue body MUST cite: (a) the parent spec path `specs/2026-06-18-issue-361-orchestrator-service/`, (b) the boundary type from FR-001, (c) the parent FR ID range owned by the child (e.g., `#361.1` owns FR-002/FR-003/FR-004/FR-012(mechanism)), (d) the child-intake note from `## Notes for Child Intake` if one applies to the child, (e) an `## Activation` section stating whether the child is BLOCKED or UNBLOCKED at filing time and (when BLOCKED) the exact dependency chain a human operator MUST observe before applying `agent-ready` manually. Idempotency MUST be enforced by a pre-check that searches existing open issues by canonical title prefix (`feat(orchestrator): <scope> (Child <N> of #361)`); re-runs MUST file zero duplicates. **Label policy (per ADR-issue-337-trigger-authorization-model + Codex P1 review of PR #372 + Codex P2 re-review of PR #372).** The script MUST apply the labels `enhancement`, `infrastructure`, `priority:p1` to EVERY filed child issue. The script MUST additionally apply `agent-ready` to `#361.1` ONLY (the single unblocked child — `#361.1`'s only dependency `#360` is closed). The script MUST NOT apply `agent-ready` to `#361.2`, `#361.4`, or `#361.5` at filing time because those three children declare blockers (`#361.1` for `#361.2`/`#361.5`; `#361.1` + `#361.2` + spec-complete `#334` for `#361.4`); applying `agent-ready` to a blocked child would let factory execution start before the dependency contract is concrete. A human operator MUST apply `agent-ready` to each blocked child manually once its blocker chain clears. The script's invocation contract is documented in `pr_context.md` for the human operator to run at parent merge.
- FR-015 This work item MUST author an idempotent helper script at `specs/2026-06-18-issue-361-orchestrator-service/add_deferred_triggers.sh` that injects EXACTLY two entries into `AGENTS.backlog.md` so `#361.3` filing surfaces mechanically when its blockers resolve: (a) `proposal(issue-361-orchestrator-service): file #361.3 — RabbitMQ subscriber + OpenHands API client + work loop` with `trigger: after: issue-335`, (b) the identical entry with `trigger: after: issue-336`. **Injection convention:** each entry MUST be inserted beneath its matching `### after: issue-NNN` subsection header within the `## Parked Proposals` section. If the header exists, the entry MUST be appended under it (preserving any pre-existing entries in that subsection); if absent, the script MUST create the subsection header just before the `## Long Horizon` section. Plain append-to-end-of-file MUST NOT be used because the resulting entry would land outside the convention and bypass operator-scan-by-trigger-heading. Idempotency MUST be enforced by a pre-check that greps `AGENTS.backlog.md` for the exact entry token; re-runs MUST append zero duplicates. Both entries MUST cite the parent spec path and the canonical boundary-type label `architectural-layer: infrastructure — external-runtime clients` from FR-001(c) (per ADR-issue-337-light-decomposition-policy § Allowed boundary types; the earlier shorthand `layer — external-runtime clients` is non-canonical and MUST NOT be emitted by the helper script).
- FR-016 The two helper scripts authored in FR-014 and FR-015 (`file_children.sh` + `add_deferred_triggers.sh`) are single-purpose ephemera. They serve their function at parent `#361` PR merge time (when the human operator runs them) and have zero residual value thereafter. `#361.3` (the canonically last-mergeable child by dependency-blocker chain — blocked on `#335` + `#336` spec-complete + `#361.1` + `#361.2`) MUST include a `git rm` of both scripts in its own PR scope. This binds the deletion responsibility to a single named child so the obligation cannot be diluted across the decomposition; if at parent-close time `#361.3` has not yet been filed or merged (e.g., `#335`/`#336` have not reached spec-complete by the time the other 4 children merge), the deletion MUST be authored as a follow-up document-sync PR by whichever human closes the parent `#361` issue. The `#361.3` body authored later by the deferred-trigger backlog entry (FR-015) MUST cite this responsibility explicitly so the implementer cannot miss it. The two script files MUST NOT remain in the repository after parent `#361` issue closure.
- FR-017 Parent `#361` MUST NOT be closed by GitHub auto-link keywords on any PR. Per Contract C4, parent closure is a deliberate human action AFTER (a) all 5 child PRs merge AND (b) every cross-child Integration AC checkbox on the parent issue body is ticked by a human bounded-context reviewer. To prevent accidental auto-close, the parent coordination PR (this work item) and every child PR body MUST cite parent `#361` with the informational keyword `Tracks #361` and MUST NOT cite it with any of the GitHub auto-close keywords `Closes` / `Fixes` / `Resolves` / `Close` / `Fix` / `Resolve` (case-insensitive). The ONLY auto-close keyword permitted in this decomposition is `Closes #369` on the `#361.5` PR body (a different issue scoped to the usability-pragmatist expert addition). The `file_children.sh` body templates authored under FR-014 MUST embed this rule as an explicit `## Closing` section per child; the `add_deferred_triggers.sh` rationale authored under FR-015 MUST embed the same rule in the `#361.3` filing instructions.

### Non-Functional Requirements (Normative)
- NFR-SEC-001 The orchestrator pod MUST run as a non-root user with a read-only root filesystem; the Helm chart `securityContext` MUST set `runAsNonRoot: true`, `runAsUser: 65532` (or a comparable distroless UID), `readOnlyRootFilesystem: true`, `allowPrivilegeEscalation: false`, and `capabilities.drop: ["ALL"]`. Credentials MUST be sourced only via ESO-mounted volumes (no environment-variable secret injection from K8s `Secret` env refs). The orchestrator container image MUST be distroless or equivalent; busybox/alpine shells MUST NOT be present in the runtime image.
- NFR-OBS-001 The orchestrator MUST log structured JSON to stdout for every dispatch decision, every skill invocation, every schema-validation outcome, and every C7 emission; log fields MUST include `ticket_id`, `phase`, `rerun_round`, `event_id`, `decision_summary`. The orchestrator MUST expose a Prometheus `/metrics` endpoint with counters for: dispatched skills, schema validation failures, C7 emissions by phase, panel convergence outcomes by mode. The orchestrator MUST emit the additive extension field `outcome_details.token_usage` on every panel-dispatched C7 event so per-ticket cost is queryable from the C7 stream alone (per ADR-issue-368).
- NFR-REL-001 The orchestrator MUST tolerate at-least-once delivery of `#336` trigger messages: duplicate dispatches for the same `(ticket_id, phase, rerun_round)` tuple MUST collapse to a single C7 event at the bus via the deterministic `event_id` rule. The orchestrator MUST be restart-safe: on pod restart, in-flight panel invocations MUST resume from the durable bus event log (NOT from in-memory state); the per-ticket token accumulator MUST be rebuilt from the bus on restart. Helm rolling-update MUST set `maxUnavailable: 0` and `maxSurge: 1` so the orchestrator is never offline during upgrades. Rollback is `helm rollback orchestrator <previous-revision>`.
- NFR-OPS-001 The Helm chart MUST ship a Kubernetes `Deployment` with `replicas: 1` (single-writer to the per-ticket token accumulator; horizontal scaling is out of scope for v1). Pod liveness MUST be a TCP probe on the Prometheus `/metrics` port; readiness MUST be a synthetic AMQP connection check against the durable bus. The runbook updates MUST cite: (a) how to drain the trigger queue, (b) how to read C7 events from the durable bus, (c) how to look up the most recent `phase: implement` event for reviewer-rotation debugging.
- NFR-A11Y-001 N/A — the orchestrator is a headless Python `Deployment` with no UI surface. Operator observability is via Grafana dashboards authored downstream by `#350`; that work item carries the WCAG 2.1 AA scope for the dashboard surface.

## Normative Option Decision
- Option A: Decompose `#361` into 5 children at this intake (file 5 child issues; each child receives an independent SDD intake; this parent spec carries Integration AC).
- Option B: Single spec, single PR — author one comprehensive `#361` spec covering all surfaces; flag `#335` / `#336` dependencies as explicit blocker open-questions; ship as one large PR.
- Selected option: OPTION_A
- Rationale: Per the project's light-decomposition preference (`feedback_decomposition_preference`) and the precedent set by `#333` → `#360` + `#361` split at intake, large coordination tickets that span pure-Python core logic AND external-runtime integration AND infrastructure deployment AND governance-docs roster authoring MUST be sliced early. The five boundaries align with distinct review surfaces and unblock parallel execution: `#361.1`, `#361.2`, `#361.4`, and `#361.5` proceed in parallel; `#361.3` waits on `#335` + `#336` spec-complete. The fifth child (`#361.5` usability-pragmatist expert) carries governance-docs work analogous to `#360` — bundling it with the deployment surface (`#361.4`) would force Security + Operations reviewers to context-switch into Architecture + Product PERSONA.md content review on the same PR; splitting it out keeps the architecture-sign-off exception on the 9-vs-8 expert ceiling scoped to a single PR. Fan-out of 5 hits but does not exceed the ADR-issue-337-light-decomposition-policy cap of 5.

## Contract Changes (Normative)
- Config/Env contract: The orchestrator pod environment MUST read EXACTLY the following inputs: `DURABLE_BUS_URL` (RabbitMQ AMQP URL), `OPENHANDS_AGENT_SERVER_URL`, `FACTORY_BOT_SERVICE_ACCOUNT`, `MODEL_ROUTER_CONFIGMAP_NAME`, `REVIEWER_ROTATION_CONFIGMAP_NAME`. ESO-mounted secret references are wired via volume mounts, NOT env vars (per NFR-SEC-001).
- API contract: The orchestrator is an OpenHands Agent Server API client. The client surface against `#335`'s API is consumed only; the orchestrator publishes no HTTP API of its own.
- OpenAPI / Pact contract path: none (orchestrator is an API consumer only)
- Event contract: The orchestrator emits C7 lifecycle events per `docs/blueprint/autonomous-factory/design-contracts.md` § Contract C7 (sealed eleven-field minimum + the additive extension fields listed in FR-006); no change to the C7 schema itself, which is sealed by `#339`.
- Make/CLI contract: Slice 4 adds Helm install/upgrade Make targets under the existing `infra-helm-*` family (`infra-helm-orchestrator-install`, `infra-helm-orchestrator-upgrade`, `infra-helm-orchestrator-status`) per the local-first wrapper conventions; no new top-level Make targets.
- Docs contract: `docs/blueprint/autonomous-factory/design-contracts.md` § Contract C8 gains EXACTLY ONE row for the orchestrator Helm chart per FR-010 with stability tier `stable`. `docs/blueprint/autonomous-factory/orchestrator.md` (new file in `#361.3`) MUST document the work loop, the dispatch matrix loader, the convergence engine, the schema validator, the C7 emitter, the reviewer-rotation picker, the conditional-dispatch predicate registry, and the operator runbook entries from NFR-OPS-001.

## Blueprint Upstream Defect Escalation (Normative)
- Upstream issue URL: none
- Temporary workaround path: none
- Replacement trigger: none
- Workaround review date: none

## Normative Acceptance Criteria
- AC-001 [Dispatch matrix loader reads § C3 and rejects unknown skills/experts at startup] — verified by T-101, which MUST assert that the orchestrator startup raises a `DispatchMatrixError` when invoked against a fixture `design-contracts.md` containing a row with a fabricated skill basename absent from `.agents/skills/` AND that an identical assertion holds for a fabricated `expert_slug_blueprint` value absent from the sealed blueprint-baseline roster (per the C7 `expert_verdicts[]` two-sub-enum schema; values matching the consumer overlay's `expert_slug_extension` allowlist MUST be accepted instead of rejected), AND that the loader returns a typed mapping covering EXACTLY the 8 SDD steps `step01..step08` when invoked against the canonical `docs/blueprint/autonomous-factory/design-contracts.md` § C3 (the loader's row-count assertion MUST be derived from the parsed step-set, NOT a literal `len(rows) == 8` constant; FR-012's `usability-pragmatist` rows authored in `#361.5` add up to 4 additional gated rows for step01/04/05/08, so the canonical matrix carries 12 rows post-`#361.5` while still covering the same 8 SDD steps — fixing the row-count assertion to "EXACTLY 8 rows" would make T-101 fail the moment `#361.5` lands, the exact dead-on-arrival pattern Codex flagged on PR #372 6th-review).
- AC-002 [Three convergence modes produce the documented merger outputs] — verified by T-102, which MUST assert: (a) `parallel-then-merge` collapses a panel verdict list `[pass, revise, block, pass]` to aggregate `block` with `findings_before_dedup = 4` and `findings_after_dedup` matching the post-dedup count; (b) `sequential-lens` applied to step05 invokes the 5 experts from § C3 row 5 in the documented order, passing each expert the prior expert's deltas via the invocation contract; (c) `structured-disagreement` applied to step08 surfaces a cross-lens contradiction when two experts return conflicting `block` verdicts on the same finding category by emitting both `expert_verdicts[]` rows and recording `severity_escalation_events ≥ 1` on `merger_overhead`.
- AC-003 [Schema validator rejects skill output that violates the SKILL.md jsonschema and suppresses C7 emission] — verified by T-103, which MUST assert that a fixture skill output missing a required field declared in that skill's `## Required Output Schema` fenced block causes (a) the orchestrator to raise `SchemaValidationError`, (b) NO C7 event is published to the bus stub, (c) the failure is recorded in the structured stdout log with `decision_summary: schema-validation-failed` AND the OpenHands execution-log shim records the failure.
- AC-004 [Emitted C7 event carries all 11 sealed fields, the deterministic event_id, AND the 4 additive extension fields on a panel-dispatched phase] — verified by T-104, which MUST assert that emitting a `phase: intake` event with fixture inputs `ticket_id=361, parent_ticket_id=333, rerun_round=0, owner_team=@sbonoc/factory-architecture` produces a bus message whose JSON body (a) contains EXACTLY the eleven sealed minimum keys with the exact types from § C7, (b) carries `event_id == sha256("361|intake|0|orchestrator").hexdigest()`, (c) carries `emitter: orchestrator`, (d) carries `outcome_details.expert_verdicts[]` with 8 rows (one per dispatched expert for step01), (e) carries `outcome_details.routing_keys[]` of length ≥ 1 with LiteLLM-prefixed values, (f) carries `outcome_details.token_usage` keyed by `expert_slug` with at least one integer counter, (g) carries `outcome_details.merger_overhead` with the three required integer keys.
- AC-005 [Integration AC — end-to-end work-item dispatch produces every expected C7 phase event] — verified by T-201 (cross-child integration test), which MUST assert that issuing a single fixture `trigger-accepted` message for `ticket_id=demo-1` through child `#361.3`'s RabbitMQ subscriber and child `#361.2`'s emitter results in exactly 8 C7 events on the bus (one per SDD step boundary), each carrying `emitter: orchestrator` and a distinct deterministic `event_id`. (This AC is satisfiable only by cross-child behavior — `#361.1` dispatches, `#361.2` emits, `#361.3` triggers — and MUST be ticked by a human bounded-context reviewer per Contract C4.)
- AC-006 [Integration AC — reviewer-rotation picker selects a heterogeneous panel] — verified by T-202 (cross-child integration test), which MUST assert that with a fixture prior `phase: implement` event carrying `model: claude-opus-4-7` on the bus, dispatching `phase: agent-pr-review` for the same `ticket_id` produces an `outcome_details.routing_keys[]` set whose `model_family(s)` normalization includes at least one routing key NOT in `{opus}` AND the set contains ≥ 2 distinct families.
- AC-007 [Integration AC — orchestrator deploys via Helm, runs as non-root, and the NetworkPolicy blocks public egress] — verified by T-203 (cross-child integration test), which MUST assert that (a) `helm install orchestrator scripts/templates/infra/orchestrator/` succeeds against a fixture kind cluster, (b) the pod's `securityContext` satisfies NFR-SEC-001, (c) the NetworkPolicy denies a probe egress connection to a public-internet IP from the orchestrator pod.
- AC-008 [Integration AC — schema validation failure surfaces through the #336 reject-rerun cap path] — verified by T-204 (cross-child integration test), which MUST assert that a deliberate skill-output schema violation triggered through child `#361.3` produces (a) no C7 event, (b) a reject signal consumed by the `#336` webhook handler stub, (c) the reject counter increments correctly against the FR-008 rerun cap.
- AC-009 [Integration AC — `ui-fidelity-or-a11y` predicate disjunction gates `usability-pragmatist` correctly on three fixture cases] — verified by T-205 (cross-child integration test), which MUST assert that (a) dispatching step01 on a fixture work item with `has-user-facing-flow: false` AND a pure-backend diff produces an `expert_verdicts[]` array that does NOT contain a row keyed `usability-pragmatist`; (b) dispatching step01 on a fixture work item with `has-user-facing-flow: true` AND a frontend-path diff produces an `expert_verdicts[]` array that DOES contain a row keyed `usability-pragmatist` (first OR-clause: new user-facing flow); (c) dispatching step01 on a fixture work item with `has-user-facing-flow: false` AND a diff containing ONLY accessibility-relevant paths (e.g., `app.css` contrast fix, `aria-label` addition) produces an `expert_verdicts[]` array that DOES contain a row keyed `usability-pragmatist` (second OR-clause: accessibility-only change to existing surface). (Closes the previously deferred `#369` follow-on.)
- AC-010 [`file_children.sh` is idempotent and produces 4 well-formed issue bodies with per-child label policy] — verified by T-110, which MUST assert that against a stub `gh` CLI fixture: (a) the first invocation produces EXACTLY four `gh issue create` calls — one per child slug in `{#361.1, #361.2, #361.4, #361.5}`, NO call for `#361.3`; (b) each issue body contains the literal substring `specs/2026-06-18-issue-361-orchestrator-service/`, a boundary-type label matching FR-001 row, the parent FR ID range string, any applicable `## Notes for Child Intake` text, AND an `## Activation` section per FR-014 (e); (c) the per-child label policy from FR-014 holds: the `gh issue create` call for `#361.1` carries EXACTLY the four labels `agent-ready,enhancement,infrastructure,priority:p1`, AND the `gh issue create` calls for `#361.2` / `#361.4` / `#361.5` carry EXACTLY the three labels `enhancement,infrastructure,priority:p1` (NO `agent-ready` at filing time); (d) a second invocation of the script against the same stub state (with the 4 previously-created issues returned by the stub's `gh issue list` pre-check) produces zero new `gh issue create` calls; (e) script exit code is 0 on both runs.
- AC-011 [`add_deferred_triggers.sh` is idempotent and appends 2 backlog entries] — verified by T-110, which MUST assert that against a fixture `AGENTS.backlog.md`: (a) the first invocation appends EXACTLY two entries containing the literal strings `trigger: after: issue-335` and `trigger: after: issue-336`, both citing the parent spec path; (b) a second invocation against the post-first-run fixture appends zero new entries; (c) script exit code is 0 on both runs.
- AC-012 [`#361.3` PR removes both helper scripts] — verified by T-211 (cross-child integration check authored in `#361.3`), which MUST assert that after `#361.3` is merged, `specs/2026-06-18-issue-361-orchestrator-service/file_children.sh` and `specs/2026-06-18-issue-361-orchestrator-service/add_deferred_triggers.sh` MUST NOT exist in the repository working tree on `main`. The `#361.3` PR diff MUST include the `git rm` of both files.
- AC-013 [No child body template auto-closes `#361`] — verified by T-110 (extended), which MUST assert that every issue-create call recorded by the gh stub during a `file_children.sh` first-run has a `--body` value that (a) contains the literal substring `Tracks #361`, (b) does NOT contain any of the case-insensitive substrings matching the regex `(?i)\b(close[ds]?|fix(e[ds])?|resolve[ds]?)\s+#361\b`. The same regex MUST be asserted absent from the `add_deferred_triggers.sh` `#361.3` rationale text appended to the fixture backlog file.

## Informative Notes (Non-Normative)
- Context: `#361` is Child B of `#333` (`#332` Phase 1). Child A `#360` closed 2026-06-03 (personas + skill runbooks + jsonschema blocks ready for the orchestrator to consume). The orchestrator's runtime targets are owned by sibling tickets that are still OPEN: `#335` (OpenHands Agent Server + LiteLLM gateway) and `#336` (RabbitMQ trigger queue topology + webhook handler). Per the user decision at this intake, `#361` is decomposed into 5 children so the pure-core slices can ship while the runtime-client slice waits on `#335` + `#336` spec-complete. The `#369` UX/UI expert follow-on is closed inside `#361.5` (governance-docs boundary, parallel to the `#360` precedent), depending only on `#361.1` for the predicate-registry mechanism — not bundled with the deployment surface `#361.4` so the architecture-sign-off exception on the 9-vs-8 expert ceiling stays scoped to a single PR.
- Sealed-lifecycle assumption: All 5 children assume the current sealed 8-step SDD lifecycle (`step01-intake` → `step08-agent-pr-review`) per `docs/blueprint/autonomous-factory/design-contracts.md` § C7 `phase` enum (8 SDD-step values plus the `c7-emission-opted-out` local-cli sentinel) and § C3 dispatch matrix (8 rows). Load-bearing FRs/ACs encode this count explicitly: FR-002 (loader rejects any matrix that drifts from the 8 sealed skill basenames + the standing `expert_slug_blueprint` enum, where the latter is widened from 8 to 9 by `#361.5`'s paired enum amendment per FR-012; future blueprint expert additions continue to require a `#339` cycle while consumer-specific expert additions go through the additive `expert_slug_extension` overlay without a sign-off cycle per design-contracts.md § C7 F-12 amended 2026-06-19), FR-003 (convergence-mode assignment by step number: parallel-then-merge on steps 01/02/04/06/07, sequential-lens on step05, structured-disagreement on step08), AC-001 (loader returns EXACTLY 8 rows). Lifecycle extension is gated by a #339 sign-off cycle (per design-contracts.md line 319 — "additions to the enum are out of scope for consumer overlays"); if the lifecycle ever grows post-merge (e.g., a hypothetical `step09-post-merge-monitor`), all 5 children MUST be amended to handle the new step's matrix row, convergence-mode assignment, and `phase` enum value. This anchoring is intentional — the orchestrator MUST fail closed on matrix drift so a silent C3 shape change cannot bypass the sealed-emitter discipline.
- Tradeoffs: Decomposing into 5 children adds parent-spec coordination overhead but unblocks parallel review surfaces (Architecture, Product, Operations, Security) and avoids a single mega-PR. The cost is one additional Integration AC ticking pass at parent-close time per Contract C4.
- Clarifications:
  - **Q-1 RESOLVED 2026-06-18 (Option B; amended 2026-06-18 for 5-child split)** — Filing of the 5 child GitHub issues: file `#361.1` + `#361.2` + `#361.4` + `#361.5` at parent merge (independent of `#335` / `#336` spec-complete state); defer `#361.3` filing until `#335` + `#336` reach spec-complete so the runtime-client child's API contracts are concrete. Add `after: issue-335` + `after: issue-336` entries to `AGENTS.backlog.md` at parent merge so `#361.3` filing is mechanically triggered. Rationale: parallelism beats discipline because parent FRs derive entirely from already-merged ADRs (#337/#339/#360/#364/#368), so there is no meaningful drift risk between PR #372 open and merge. Decision by PO comment 2026-06-18 (https://github.com/sbonoc/stackit-platform-blueprint/pull/372#issuecomment-4743504901); 5-child split amendment applied 2026-06-18 after PO concurrence on splitting `usability-pragmatist` governance-docs scope out of the deployment-surface child.
  - **Q-2 RESOLVED 2026-06-18 (Option A)** — Workspace pod lifetime per OpenHands invocation: per-`(ticket_id, phase)` ephemeral workspace pod. Surfaces as an explicit `#335` spec requirement that the Agent Server API exposes per-invocation workspace creation, so the API surface is co-designed rather than retro-fitted. Rationale: crash isolation + schema-validation discipline are first-order properties of factory auditability; Option A's blast-radius bound is more valuable than Option B's cold-start savings when the system is being debugged. Decision by PO comment 2026-06-18 (https://github.com/sbonoc/stackit-platform-blueprint/pull/372#issuecomment-4743504901).
  - **Q-3 RESOLVED 2026-06-18 (Option A; amended 2026-06-19 to widen predicate to OR-form for accessibility-only changes)** — Conditional-dispatch predicate evaluation surface: typed `Predicate` object referenced by name from each C3 matrix row; orchestrator passes a `WorkItemContext` with `changed_paths: list[str]` derived from `git diff --name-only base..head` against the PR head SHA; TWO consumer-extensible regex sets live in the orchestrator config-map (`predicates.frontend.pathRegex` default `^(touchpoints/|frontend/|.*\.vue|.*\.tsx)$` AND `predicates.accessibility.pathRegex` default `^(.*\.(css|scss)|.*aria.*|.*axe.*|.*a11y.*|.*accessibility.*)$`); both config-map regexes MUST be validated at orchestrator startup (fail-fast) to prevent a bad regex from breaking every dispatch. Rationale: the predicate evaluates as `( has-user-facing-flow: true AND diff-touches-frontend-paths ) OR diff-touches-accessibility-relevant-paths` — the OR-form covers BOTH new-user-facing-flow work (first clause) AND accessibility-only changes to existing surfaces (second clause; CSS contrast fixes, ARIA semantics, focus-management bug fixes that legitimately declare `has-user-facing-flow: false`). The strict-AND form would have silently let accessibility-only PRs ship without `usability-pragmatist` review — precisely the case where the persona matters most. The two-clause disjunction justifies the 9-vs-8 expert-ceiling exception on `usability-pragmatist`; single-input variants have clear false-fire paths that waste Opus tokens on non-applicable work. Decision by PO comment 2026-06-18 (https://github.com/sbonoc/stackit-platform-blueprint/pull/372#issuecomment-4743504901); OR-widening amendment applied 2026-06-19 after PO concurrence that accessibility-only changes (CSS, ARIA, axe-config) MUST trigger the persona regardless of `has-user-facing-flow`.
  - **Q-4 RESOLVED 2026-06-18 (Option A)** — Helm chart values for ESO-mounted durable-bus credentials: one `eso.clusterSecretStoreRef` (default `stackit-secrets-manager`) plus three separate `eso.secretKeyRef.{bus,openhands,litellm}` references mounted at `/var/run/secrets/orchestrator/{bus,openhands,litellm}` and read via three independent `EsoSecretVolumeReader` instances. Rationale: per-credential rotation independence without coupling unrelated credentials (a single shared `ExternalSecret` would block all three credentials if any one key fails to sync); matches the observability-csi-hardening pattern adopted on issue #312. Decision by PO comment 2026-06-18 (https://github.com/sbonoc/stackit-platform-blueprint/pull/372#issuecomment-4743504901).

## Notes for Child Intake (Non-Normative)

Captured at parent intake from the boundary review of the 5-child decomposition. Each child's own SDD step01 intake MUST fold the relevant note into its `spec.md` before opening its Draft PR. These notes are non-normative on this parent spec but become normative inputs to each child's intake.

- **For `#361.1` intake — fixture-predicate test strategy.** The predicate-registry mechanism (FR-012) lives in `#361.1` but its first real predicate (`has-user-facing-flow + diff-touches-frontend-paths`) is authored in `#361.5`. The implementer MUST add at least one fixture predicate (e.g., `always_true`, `always_false`) inside `#361.1`'s own test suite so the registry can be exercised without depending on `#361.5`. Reaching into `#361.5`'s PERSONA.md or matrix row mid-implementation is forbidden — the dependency direction is `#361.5` depends on `#361.1`, not the reverse.
- **For `#361.3` intake — `3a` / `3b` re-split contingency.** `#361.3` has two upstream blockers (`#335` spec-complete AND `#336` spec-complete). If the blocker-resolution dates diverge by more than ~2 weeks, the child intake MUST re-evaluate splitting into `#361.3a` (OpenHands client + work-loop skeleton) and `#361.3b` (RabbitMQ subscriber + work-loop completion). Default position: keep as one child — the work loop is the integration point and a half-finished client PR creates a temptation to merge incomplete runtime code into main. Re-split only if the time gap forces idle reviewer capacity.
- **For `#361.5` intake — formalize the expert-ceiling exception as an ADR amendment.** The 9-vs-8 expert-roster exception (adding `usability-pragmatist` to the ADR-issue-364 ceiling of 8) MUST be authored as an ADR amendment, NOT as informal prose in the PERSONA.md front-matter. Choose EXACTLY ONE OF: (a) a `Status: amended` note on `ADR-issue-364-expert-persona-model.md` with an `Amendments` section citing this exception and the FR-012 predicate as the conditioning safety property, OR (b) a new narrowly-scoped `ADR-issue-361.5-usability-pragmatist-ceiling-exception.md` that supersedes the ADR-issue-364 § Ceiling subsection for the `usability-pragmatist` case only. The PERSONA.md front-matter cites the chosen ADR by path; the ADR carries the normative rationale.
- **For `#361.5` intake — paired C7 `expert_slug_blueprint` enum amendment (CROSS-CUTTING, MUST NOT be missed).** Per FR-012, `#361.5`'s PR scope MUST author THREE coupled artifacts atomically: (1) the `usability-pragmatist` PERSONA.md, (2) the C3 matrix wiring + Predicate-column schema change, AND (3) the paired amendment to `docs/blueprint/autonomous-factory/design-contracts.md` § C7 widening the sealed `expert_slug_blueprint` sub-enum from 8 to 9 (admitting `usability-pragmatist`). `usability-pragmatist` lands in `expert_slug_blueprint` (not the open `expert_slug_extension` overlay) because it is a blueprint-baseline expert shipped to all consumers. Without (3), the orchestrator's FR-002 loader rejects the new matrix row at startup AND the C7 emitter constructs events that fail schema validation — `#361.5` is dead-on-arrival. The enum-widening amendment goes through the `#339` sign-off cycle (per design-contracts.md § C7 F-12 clarification amended 2026-06-19). `#361.5`'s PR body MUST cite this triple-deliverable explicitly so reviewers verify all three land together; if any of the three slips to a follow-up PR, the orchestrator runtime is broken between merges. Cross-references: ADR-issue-364 § 9 (`ExpertVerdictSummary` two-sub-enum schema), design-contracts.md § C7 (sealed `expert_slug_blueprint` enum row), FR-002 (loader validates against the design-contracts enum).
- **For `#361.5` intake — `usability-pragmatist` PERSONA.md seed direction.** The PERSONA.md authoring is `#361.5`'s scope, but the parent seeds the lens to prevent intent-drift. (a) **Worldview tagline (seed):** "Given we are shipping a user-facing change, the human interface (UI fidelity + UX coherence + accessibility) MUST be inclusive and consistent enough to actually be usable — balanced pragmatically against shippability the same way `product-pragmatist` balances polish against shipping speed." (b) **Boundary vs `product-pragmatist`:** `product-pragmatist` asks the question "is this worth shipping at all, and what is the smallest valuable cut?" and pushes back on scope/value/business-outcome/time-to-ship/gold-plating. `usability-pragmatist` asks the question "given we are shipping, is the interface coherent and inclusive enough to actually be usable?" and pushes back on the interaction surface only. The two personas are deliberately complementary — both share the `-pragmatist` lens convention (ideal vs shippable balance) but operate on disjoint push-back trigger sets. (c) **Seed push-back trigger set (10 items, every one a first-class trigger — accessibility is NOT a sub-bullet under UX):** design-token drift; conflicting interaction patterns; missing loading/empty/error states; missing keyboard operability (WCAG SC 2.1.1); missing focus indicators (SC 2.4.7); missing ARIA roles/labels (SC 4.1.2 Name/Role/Value); missing programmatic name-role-value; missing color-independence (SC 1.4.1); missing error identification (SC 3.3.1); cognitive-load spikes (modal-stacking, jargon, unrecoverable destructive actions). (d) **Activation predicate:** `ui-fidelity-or-a11y` per FR-012 (OR-form covers both new UI/UX flows AND accessibility-only changes to existing surfaces). (e) **What this persona is NOT:** NOT a stand-in for a human "UX/UI Designer" job role (avoiding the stage-persona anti-pattern from ADR-issue-360 superseded by ADR-issue-364 — the slug `usability-pragmatist` deliberately names a *lens*, not a *role*; no HITL puts "Usability Pragmatist" on their LinkedIn). This seed direction is non-normative on this parent spec but becomes normative input to `#361.5`'s PERSONA.md authoring — the implementer MUST treat the 10-item trigger set as a floor (refining categorization is permitted; narrowing the scope below the UI+UX+a11y trio is NOT).
- **For ALL children — no auto-close keyword targeting parent `#361` (FR-017).** Every child PR body MUST cite parent `#361` as `Tracks #361` (informational only). It MUST NOT use any GitHub auto-close keyword (`Closes` / `Fixes` / `Resolves` / case-insensitive variants) targeting `#361`. Parent close is a deliberate human action after Contract C4 Integration AC tick-off. The SINGLE permitted auto-close keyword in this decomposition is `Closes #369` on the `#361.5` PR body — `#369` is a different issue legitimately resolved by usability-pragmatist expert addition.
- **The post-merge operator runbook in `pr_context.md` § Operator Runbook is a TEMPORARY measure, NOT a precedent.** Per PR #372 strategic-alignment audit 2026-06-19 against Epic #332 "two human gates only" vision: the three manual steps (run `file_children.sh` + run `add_deferred_triggers.sh` + manually inject Integration AC into the `#361` issue body) are a third human-touchpoint between gate-1 (spec sign-off) and gate-2 (bounded-context merge) — contradicting Epic #332's "every step between the two gates autonomous" architecture. The runbook exists only because `#336` (webhook handler) has not shipped yet. A backlog entry parked under `AGENTS.backlog.md` § `### after: issue-336` (`absorb #361 parent-merge operator runbook into #336 webhook handler on pr-merged event`) requires `#336`'s scope to absorb these three steps into the `pr-merged` webhook flow when it ships. Future parent coordination PRs (when the runtime decomposer skill ships through `#361.1`+`#361.5`) MUST NOT replicate the manual runbook pattern — they MUST delegate to `#336`'s webhook absorption. The two helper scripts shipped here (`file_children.sh` + `add_deferred_triggers.sh`) are designed to be webhook-invocable (idempotent + env-overridable + token-driven) so `#336`'s absorption is a mechanical wiring task, not a re-implementation.
- **For `#361.3` intake — `git rm` both ephemeral helper scripts (FR-016).** `#361.3` is the canonically last-merging child by dependency chain. Its PR scope MUST include `git rm specs/2026-06-18-issue-361-orchestrator-service/file_children.sh` AND `git rm specs/2026-06-18-issue-361-orchestrator-service/add_deferred_triggers.sh`. Verified by AC-012 / T-211 (authored inside `#361.3`'s own test suite, NOT in this parent work item). Do NOT leave the scripts in the tree — they reference live state and would silently misbehave if re-run weeks after parent close.


- Excluded item 1: The OpenHands Agent Server itself (owned by `#335`) and the RabbitMQ trigger queue topology + webhook handler (owned by `#336`). The orchestrator is a client of both; this work item does NOT author the server-side runtime.
- Excluded item 2: The LiteLLM gateway. All model invocations route through LiteLLM but the gateway is owned by `#335`. The orchestrator MUST NOT hold provider credentials directly.
- Excluded item 3: Embedding-based finding-text deduplication. ADR-issue-364 § 11 explicitly defers this; v1 uses naive string-equality dedup. Surfaces if dedup proves insufficient — backlog entry `proposal(issue-368-factory-cost-telemetry-routing-fixture): embedding-based router implementation` covers the related decision.
- Excluded item 4: Per-expert prompt-cache discipline. Cache discipline is observability-driven; the backlog entry `proposal(issue-368-factory-cost-telemetry-routing-fixture): per-expert prompt-cache efficiency` carries the trigger.
- Excluded item 5: Consumer-side C7 ingest + Grafana dashboard. Owned by `#350` (downstream of this work item).
- Excluded item 6: The 9th expert beyond `usability-pragmatist`. The expert ceiling is sealed at 8 by ADR-issue-364; pulling in `usability-pragmatist` brings the standing roster to 9 ONLY if approved as an exception. Resolution path: this work item proposes the addition via the FR-012 conditional-dispatch closure and the architecture-sign-off review.

## Potential Deferred Proposals
- Horizontal-scaling for the orchestrator pod: v1 ships `replicas: 1` (single-writer to the per-ticket token accumulator). Multi-replica with a shared per-ticket lock service is deferred until throughput needs are observed.
- Orchestrator local-cluster smoke lane: the orchestrator's runtime depends on `#335` + `#336` services that have no production-equivalent local Helm chart at intake time. A local-cluster smoke lane is deferred until those services ship.
- Promoting the parent `#361` coordination spec to a step04 plan-slicer execution: the 5-child decomposition declared here is the manual equivalent of `blueprint-ticket-decompose-light`; once that skill ships (governance authored in `#360`, runtime owned by this work item), the parent decomposition can be replayed through the skill for symmetry-of-evidence.
