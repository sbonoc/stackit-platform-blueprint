defaults:
  model: gpt-5
  reasoning: high
  default_prompt: "Use $blueprint-consumer-ops to run deterministic generated-consumer operations (preflight, profile runbooks, module lifecycle, runtime identity reconciliation, app onboarding, upgrade/resync, validation matrix, release readiness, troubleshooting artifacts) while preserving ownership boundaries."
