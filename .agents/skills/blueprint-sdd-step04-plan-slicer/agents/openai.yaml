interface:
  display_name: "Blueprint SDD Step 05 — Plan Slicer (Optional)"
  short_description: "Refine approved specs into dependency-ordered implementation slices with owners and validation strategy"
  default_prompt: "Use $blueprint-sdd-step04-plan-slicer to refine plan.md into dependency-ordered slices with explicit owners and per-slice validation strategy, then commit to the existing Draft PR branch."
